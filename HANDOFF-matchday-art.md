# 🎨 언더덕 FC — 경기 카드 배경 그림 (fal.ai) 핸드오프

> 경기 카드에 깔리는 배경 그림을 만들고 붙이는 전체 흐름.
> 현재 **55장** 확보(2026-08-05). 목표는 **100장**.
> 프롬프트를 다시 쓰기 전에 **"⚠️ 비싸게 배운 것"** 을 먼저 읽으세요.
> 오늘 실패한 시도들이 거기 전부 정리돼 있습니다.

## 한 줄 요약

fal.ai 로 **미리 뽑아서 커밋**한다. 런타임에는 API 를 부르지 않는다.
피드가 뜰 때마다 외부 API 를 기다릴 이유가 없고, 같은 카드가 매번 다른 그림이
되면 "디자인"이 아니라 "노이즈"가 된다.

## 🔑 환경 세팅 (체크리스트)

- [ ] `.env.local` 에 `FAL_KEY=...`
- [ ] `test/` 폴더에 레퍼런스 원본이 있는가 — **`.gitignore` 처리돼 있다(31MB)**
  - ⚠️ **레포를 새로 클론하면 이 폴더가 없어서 그림을 못 뽑는다.**
    2026-08-05 기준 원본은 작업자 맥 로컬에만 있다. 다른 PC 로 옮기려면 따로 복사할 것.
- [ ] `npm i` (스크립트가 `sharp` 를 쓴다 — 이미 의존성에 있음)

## 실행

```bash
npm run gen:matchday                 # 전부
npm run gen:matchday -- flag         # flag 묶음 전부
npm run gen:matchday -- flag 3       # flag 앞 3장
npm run gen:matchday -- flag 9,10    # flag 9번·10번만 (다시 뽑기)
```

한 장씩 다시 뽑을 땐 인덱스를 쉼표로 찍는다. `-- flag 9,9` 처럼 하나만도 된다.

## 핵심 파일

| 파일 | 역할 |
|---|---|
| `scripts/gen-matchday-art.mts` | 생성 스크립트. 프롬프트·규칙이 전부 여기 |
| `public/matchday/*.webp` | 결과물(커밋 대상) |
| `app/lib/matchday-art.ts` | **목록 등록 + 어떤 그림을 쓸지 고르는 규칙** |
| `test/*` | 레퍼런스 원본 (gitignore) |
| `public/underducklogo.png` | 크레스트 레퍼런스 |

## 두 모델을 쓴다

| | 모델 | 언제 |
|---|---|---|
| 맨바닥 생성 | `fal-ai/flux-pro/v1.1-ultra` | 레퍼런스가 필요 없을 때 |
| 레퍼런스 편집 | `fal-ai/nano-banana/edit` | `ref` 를 넘길 때 (데이터 URI 로 전달) |

`Shot.ref` 에 경로를 넣으면 자동으로 편집 모델로 간다.
**"지어내면 안 되는 것"은 반드시 `ref` 로 넣는다** — 크레스트, 우리 팀 모습, 실제 인물.

## 묶음(stage) 구성 — 현재 55장

| stage | 장수 | 성격 | 레퍼런스 |
|---|---|---|---|
| `flag` | 15 | 크레스트가 박힌 것 (깃발·트로피·엠블럼·티포·자수·부감) | `public/underducklogo.png` |
| `team` | 18 | 팀 사진을 화풍·세계째로 변환 | `test/` 팀 사진 |
| `fun` | 12 | 위트 — 확실히 "그림"인 계열 | `test/` 여러 장 |
| `mono` | 5 | 흑백 + 핑크 한 색 | `test/` 팀 사진 |
| `gk` | 3 | 골키퍼 | `test/박영휘 1.png` |
| `duo` | 2 | 2인 컷 | `test/parkhwang.png`, `test/leemkim.png` |

> `flag` 만 특별하다. **끝난 경기 카드는 `flag` 계열에서만 뽑는다**
> (`matchday-art.ts` 의 `RESULT_ART`). 100장으로 늘릴 때 `flag` 를 넉넉히
> 확보해야 마이페이지 그리드에서 같은 그림이 인접해 뜨는 걸 줄일 수 있다.

## 규칙 상수 (스크립트 안)

프롬프트는 `프롬프트 + LOOK(화풍) + RULES(금지사항)` 으로 합쳐진다.
`Shot` 마다 `look` / `rules` 를 따로 줄 수 있다.

| 상수 | 쓰는 곳 | 핵심 |
|---|---|---|
| `TRANSFORM` | 팀 사진 계열 | **"필터 씌운 사진처럼 보이면 안 된다"** — 킷·사람만 지키고 시간·날씨·빛·배경·매체는 다 바꿔라 |
| `TEAM_RULES` | 팀 사진, 얼굴 나와도 됨 | 얼굴을 **확실히 그려라**(비우지 마라) |
| `TEAM_RULES_QUIET` | 팀 사진, 미니멀 | 얼굴이 주인공이 아니게 + 여백 강제 |
| `FLAG_RULES` | 크레스트 그대로 | 크레스트를 **다시 그리지 마라** |
| `FLAG_RULES_STYLIZED` | 크레스트 양식화 | 화풍은 따르되 구성 요소는 하나도 빼지 마라 |
| `COACH_RULES` | 감독 | ⚠️ `TRANSFORM` 을 **안 쓴다**. 바꾸지 말고 옷만 갈아입혀라 |
| `MONO_LOOK` | 흑백 묶음 | 흑백 + 핑크 한 색만 |

---

## ⚠️ 비싸게 배운 것 (프롬프트 쓰기 전 필독)

### 1. D-day 숫자와 로고를 그림에 굽지 마라

확산 모델은 글자를 뭉갠다. 그리고 **예정 경기는 전부 피드에 올라와서 D 값에
상한이 없다** (D-21 카드도 생긴다). 구워 두면 대응 못 하는 값이 생긴다.
숫자는 화면에서 얹는다(`MatchFeed`). 크레스트는 반대로 **레퍼런스로 넣어** 그림
안에 박는다 — 말로 설명하면 매번 다른 오리가 나온다.

### 2. "구도를 유지하라"고 하면 사진에 색만 입힌 게 나온다

처음에 `"keep the people, their poses and the overall composition"` 이라고 썼더니
원본 사진에 필터만 씌운 결과가 나왔다. **지킬 것과 바꿀 것을 분명히 나눠야 한다**
(`TRANSFORM` 참고). 우리가 레퍼런스에서 가져오려는 건 사진이 아니라 **"우리 팀이라는
사실"** 뿐이다.

### 3. "얼굴을 흐리게/지워라"는 절대 금지

그렇게 시켰더니 **살색 빈 타원**만 남겨 놨다. 귀신 사진이 된다.
얼굴은 두 갈래로만 간다:
- **얼굴이 안 보이는 구도** — 뒷모습·원거리·역광 실루엣 (문제를 안 만든다)
- **얼굴이 나오면 화풍을 끝까지 민다** — 확실한 그림이면 뇌가 그림으로 받아들인다

어중간하게 사진과 그림 사이에 있을 때만 이질감이 생긴다.

### 4. "한국인"은 프롬프트로 안 된다

`"Korean, East Asian, black hair"` 를 다 넣어도 서양 팀 사진이 나왔다.
**레퍼런스 사진을 넣는 것만이 해법이다.** 실제 킷·체형·동네 구장(뒷산·아파트)까지
따라온다.

### 5. 시점을 바꾸려면 장면을 먼저 세워라

`"aerial drone photography looking straight down"` 만 쓰니 시점이 통째로 무시되고
로고가 허공에 떴다. 페널티박스·센터서클 같은 **장면 요소를 먼저 깔고** 크레스트를
"그 잔디 위에 눕힌 사물"로 넣으니 원근이 잡혔다(`flag-14`).

### 6. 실사로 갈 때도 "장면"을 요구해라

`"photorealistic editorial portrait"` 만 쓰니 레퍼런스 누끼를 검은 배경에 그대로
얹고 끝냈다. **"경기장으로 걸어 나오는 장면"** 처럼 상황을 줘야 세계가 지어진다.

### 7. 중요한 제약은 컷마다 반복해라

공통 `RULES` 에만 넣으면 샌다. 골키퍼 초록 유니폼이 두 번이나 필드 킷으로
바뀌었다. `"GREEN goalkeeper jersey and GREEN gloves — never the black and pink
outfield kit"` 를 **각 컷 프롬프트에 직접** 박고 나서야 잡혔다.

### 8. 인물이 실제와 다르면 "변환"이 아니라 "편집"이다

감독 컷이 다섯 번 실패했다. 원인은 **레퍼런스가 뒷모습이라 얼굴이 아예 없다**는
것이었다 — 모델이 그린 얼굴은 전부 지어낸 것이라 닮을 수가 없었다.
결국 `TRANSFORM` 을 빼고 "자세·구도 그대로, 옷만 정장으로" 로 바꿔서 겨우 나왔다.
그래도 안경을 씌우는 건 못 막았다. **이런 건 포토샵이 빠르다.**

### 9. 구도는 D-day 숫자 자리를 비워야 한다

숫자는 카드 **가운데**, 로고는 아래. 프롬프트에 `"the upper half stays open"`,
`"subject placed in the lower third"` 를 넣어야 글자가 읽힌다.
인물이 화면을 꽉 채우면 숫자가 얼굴에 겹친다.

---

## 뽑은 뒤: 목록에 등록하기

`app/lib/matchday-art.ts` 의 `ART` 배열에 추가한다. **파일과 목록이 어긋나면
카드마다 404 가 나간다.** 실제 파일 기준으로 다시 만드는 게 안전하다.

```ts
{ src: "/matchday/flag-16.webp" },                  // 깃발: 막을 두껍게
{ src: "/matchday/team-35.webp", soft: true },      // 이미 어두운 그림: 막을 얇게
{ src: "/matchday/flag-6.webp",  light: true },     // 밝은 그림: 글씨를 어둡게 뒤집는다
```

| 플래그 | 언제 |
|---|---|
| (없음) | 배경이 단순한 깃발 계열. 막을 두껍게 깔아야 글씨가 산다 |
| `soft` | 그림 자체가 이미 어두운 것. 같은 막을 씌우면 애써 만든 그림이 탁해진다 |
| `light` | 흰 종이·밝은 배경. 흰 글씨가 묻히니 카드가 어두운 글씨로 뒤집는다 |

## 어떤 그림을 쓸지 고르는 규칙

| 함수 | 쓰는 곳 | 특징 |
|---|---|---|
| `matchdayArt(matchId, dDay)` | 예정 경기 카드 | **날마다 바뀐다.** 카운트다운 느낌 |
| `matchResultArt(matchId)` | 끝난 경기 (피드·마이페이지·공유 카드) | **영구 고정.** 날짜를 안 섞는다 |

랜덤 함수를 쓰지 않는다. 매 렌더 랜덤이면 새로고침마다 그림이 바뀌어 산만하고,
서버·클라이언트가 달라져 hydration 이 깨진다.

- **끝난 경기는 왜 고정인가** — 지난 경기는 기록이다. 하루마다 바뀌면 "그 경기의
  사진"이 아니게 되고, 무엇보다 피드와 마이페이지가 서로 다른 그림을 띄운다
  (자정을 걸쳐 보면 사람마다 달라지기도 한다).
- **`DDAY_SALT`** — "오늘 뜬 게 별로다" 싶으면 이 숫자만 올린다. 예정 경기 그림을
  통째로 다시 섞는다. 끝난 경기는 이 값을 안 쓴다.

### ⚠️ 목록을 늘리면 기존 배정이 섞인다

`hash(...) % ART.length` 라서 **목록 길이가 바뀌면 나머지 값이 전부 달라진다.**
실제로 `flag` 를 7 → 14장으로 늘렸을 때 300경기 중 47%만 같은 그림을 유지했다.

예정 경기는 어차피 날마다 바뀌니 상관없다. **끝난 경기가 문제인데**, 지금은
"장식이라 괜찮다"로 두고 있다. 100장까지 늘리면 한 번 더 크게 섞인다.
정말 고정이 필요해지면 두 가지 길이 있다:
- 그림별 점수 방식(HRW) — 추가해도 대부분 유지(실험에서 47%→73%). 분포 재설계 필요
- 경기별 그림을 DB 에 저장 — 완전 고정. 백엔드 컬럼 추가 필요

## 용량

- 목표 **장당 120KB 이하** (`MAX_BYTES`). 스크립트가 품질을 낮춰 가며 맞춘다
- 1080×1080 webp. 초과하면 `⚠️ 목표 초과` 로 찍힌다 — 디테일 많은 그림은 못 맞춘다
- 현재 55장 3.4MB. **100장이면 약 6MB** 예상. 카드 하나당 한 장만 쓰고
  `loading="lazy"` 라 실사용엔 문제없지만 레포에는 영구히 남는다

## 100장까지 — 남은 45장 제안

| 우선순위 | 묶음 | 이유 |
|---|---|---|
| 1 | `flag` +15 | 끝난 경기가 여기서만 뽑는다. 지금 15장이라 그리드에서 자주 겹친다 |
| 2 | `mono` +10 | 작업자가 제일 선호한 결(`flag-5` 계열). 흑백이라 탁해지지 않고 팀 색이 살아난다 |
| 3 | `fun` +10 | "확실히 그림"인 계열이 curation 에서 가장 많이 살아남았다 |
| 4 | `team` +10 | 소재는 많지만 필터 티가 나기 쉬워 실패율이 높다 |

### 안 뽑는 게 나은 것 (curation 에서 전부 탈락)

- 미니멀·추상 (뭐가뭔지 모르겠고 언더덕과 무관해 보인다)
- 감독 인물 컷 (위 8번)
- 정면 단체사진에 필터만 씌운 것 (얼굴이 이질적)
- 초록 잔디가 크게 나오는 것 (네이비+핑크 팔레트가 깨진다)

## 관련 문서

- `HANDOFF.md` — 로그인·게이팅·투표·칭호
- `CLAUDE.md` — 프로젝트 전반 (⚠️ "DB = 구글 시트" 서술은 outdated. 실제는 백엔드)
